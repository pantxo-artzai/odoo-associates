<odoo>
    <data>
        <!-- Ajoutez cette action de serveur -->
        <record id="action_update_associate" model="ir.actions.server">
            <field name="name">Transfert shares</field>
            <field name="model_id" ref="model_associates_share"/>
            <field name="binding_model_id" ref="model_associates_share"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.update_associate()</field>
        </record>
    </data>
</odoo>
